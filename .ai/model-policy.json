{
  "version": "1.0.0",
  "description": "Machine-readable single source for model policy. Referenced by AI_WORKFLOW.md §14, enforced by .claude/hooks/model-lock.py. Do not duplicate these values in other documents. 'version' is the schema version: consumers support 1.x only and must fail closed on any other major version or on missing/invalid required fields. claude.session_models constrains the orchestrator session (verified behaviorally per §14-2); claude.subagent_models and claude.effort constrain subagent invocations (hook-enforced).",
  "claude": {
    "session_models": ["claude-fable-5", "claude-opus-5"],
    "subagent_models": ["fable", "opus"],
    "effort": "max"
  },
  "codex": {
    "model": "gpt-5.6-sol",
    "model_reasoning_effort": "max",
    "config_path": "~/.codex/config.toml",
    "per_call_overrides": false
  },
  "fail_closed": true
}
