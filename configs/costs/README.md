# Cost Configurations

Each file will identify a dated, verified fee schedule plus conservative spread,
slippage, and fixed operating-cost allocation assumptions.

`cost_pending_v1.toml` is an explicit blocking configuration, not a usable cost
model. Replace it with a new version only after every required numeric input is
verified; never edit a version already referenced by a run.
