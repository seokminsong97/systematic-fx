# Leakage Tests

This suite will cover future-row mutation, late-event insertion, label mutation,
split-boundary isolation, and closed-bucket immutability from
[`VALIDATION.md`](../../docs/VALIDATION.md).
