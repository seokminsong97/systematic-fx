# Server-only connection to the separate public projection database.
# Never prefix this variable with NEXT_PUBLIC_.
SITE_DATABASE_URL=postgresql://PUBLIC_READER:PASSWORD@HOST:5432/systematic_fx_public
SITE_CAMPAIGN_KEY=phase1a_conservative_screening_v1

# Client refresh cadence for already-open dashboards.
NEXT_PUBLIC_REFRESH_INTERVAL_MS=15000
